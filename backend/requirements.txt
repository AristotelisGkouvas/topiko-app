fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
# The app talks to Postgres over asyncpg, but Alembic runs its migrations
# synchronously (see sync_database_url), and that needs a sync driver of its
# own. Missing it, `alembic upgrade head` fails on a clean install with a
# ModuleNotFoundError naming a package nothing in the source imports.
psycopg2-binary==2.9.12
pydantic==2.10.4
# Auth. Argon2id is the current password-hashing recommendation and, unlike
# bcrypt, has no 72-byte input truncation to remember.
argon2-cffi==23.1.0
PyJWT==2.10.1
# Web Push. Signing a push message needs VAPID, which is ECDSA over a curve
# the standard library does not do — this pulls in `cryptography` for it.
pywebpush==2.0.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
greenlet==3.1.1
beautifulsoup4==4.12.3
pytest==8.3.4
# The scraper and the API are async top to bottom; without this their tests
# are silently *skipped* rather than failed, which is worse than not having them.
pytest-asyncio==0.25.0
httpx==0.28.1
# Kickoff times are Greek wall-clock, so the scraper needs Europe/Athens.
# Linux carries the tz database; Windows does not, and zoneinfo raises there.
tzdata==2024.2
