# Base URL of the FastAPI backend, used from both server and client components.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Which ΕΠΣ this deployment serves. One deployment = one association for now;
# ASSOCIATION in src/lib/api.ts is the one place it is read.
NEXT_PUBLIC_ASSOCIATION=epsip-ipeirou
