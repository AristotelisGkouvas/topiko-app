# Τι δεν έχει γίνει

Κατάσταση στις 24 Σεπτεμβρίου 2026, μετά τα βήματα 1–6 του
`design_handoff_pame_sentra/README.md`.

Τα νούμερα εδώ είναι μετρημένα από τη βάση, όχι εκτιμήσεις. Όπου λέει «δεν
γίνεται», λέει και γιατί.

---

## 1. Λείπουν δεδομένα, όχι κώδικας

Αυτά είναι φτιαγμένα και στυλιζαρισμένα. Απλώς δεν έχουν τι να δείξουν.

| Τι | Κατάσταση | Τι χρειάζεται |
|---|---|---|
| **Ζώνες ανόδου/υποβιβασμού** | **0 από 2.241** γραμμές βαθμολογίας έχουν ζώνη | Ο scraper δεν φέρνει τα όρια. Είναι στον κανονισμό κάθε κατηγορίας — μπαίνουν χειροκίνητα ανά διοργάνωση, μία φορά τη σεζόν. Η πράσινη/κόκκινη ράγα εμφανίζεται μόνη της μόλις υπάρχουν. |
| **Συντεταγμένες γηπέδων** | **0 από 114** | Η ένωση δεν τις δημοσιεύει. Το Nominatim επιστρέφει 403 με placeholder User-Agent. Είτε πραγματικό User-Agent με email, είτε χειροκίνητη καταχώριση από τη διαχείριση. Ο χάρτης λέει ήδη ότι είναι άδειος. |
| **Σήματα σωματείων** | **0 από 177** | Το handoff τα δηλώνει ρητά ως «λείπουν ακόμα». Ως τότε: μονόγραμμα σε χρωματιστό κύκλο, όπως λέει το design. |
| **Χρώματα σωματείων** | **0 από 177** | Ίδιο. Τα πλακίδια χρησιμοποιούν το navy της μάρκας. |
| **Φωτογραφίες** (γήπεδα, παίκτες, live) | Καμία | Δεν υπάρχει αποθήκευση αρχείων. Θέλει bucket ή δίσκο, endpoint ανεβάσματος, και απόφαση για το ποιος ανεβάζει. Στη σελίδα γηπέδου μπαίνει το διαγώνιο hatch του design. |
| **Σύντομα ονόματα σε πεζά με τόνους** | 171/177 έχουν σύντομο όνομα, **όλα ΚΕΦΑΛΑΙΑ** | Τα mockups δείχνουν «Κληματιά». Βγάζουμε «ΚΛΗΜΑΤΙΑΣ». Δύο πράγματα δεν παράγονται από το μητρώο: τα ελληνικά κεφαλαία σωστά δεν έχουν τόνο (το lower-case δίνει «Κληματιας», ανορθόγραφο), και το μητρώο γράφει τα τοπωνύμια σε **γενική** — η ονομαστική θέλει λεξικό, γιατί ο κανόνας που κάνει «ΖΙΤΣΑΣ→Ζίτσα» κάνει το «ΒΕΛΙΣΣΑΡΙΟΥ» ασυναρτησία. Λύση: 177 γραμμές ανθρώπινης πληκτρολόγησης. Η στήλη είναι ήδη επεξεργάσιμη· λείπει οθόνη στη διαχείριση. |

### Κύπελλο (οθόνες C1, CD)

**Δεν υπάρχει καμία κυπελλική διοργάνωση στα δεδομένα.** Οι 241 μη-πρωταθλήματα
που φαίνονται στη βάση είναι `championship` (209) και `playoff` (32) — κανένα
`cup`. Δεν χτίστηκε γιατί δεν θα είχε τι να δείξει, όχι επειδή είναι δύσκολο.
Όταν η ένωση δημοσιεύσει κύπελλο, το ταμπλό είναι μια μέρα δουλειάς.

---

## 2. Λείπει υπηρεσία

| Τι | Εμπόδιο |
|---|---|
| **MVP με λογαριασμό** | Το design ζητά κωδικό μιας χρήσης σε SMS ή email. **Δεν υπάρχει ούτε πάροχος SMS ούτε εξερχόμενο mail.** Η ψηφοφορία δουλεύει με ανώνυμο token browser — όποιος καθαρίσει τα δεδομένα του site ξαναψηφίζει. Αν ρυθμιστεί πάροχος, το `voter_token` γίνεται `user_id` και τίποτε άλλο δεν αλλάζει. |
| **Push ειδοποιήσεις** | Ο κώδικας είναι πλήρης (προτιμήσεις ανά ομάδα και γεγονός, ήσυχες ώρες). **Τα κλειδιά VAPID είναι κενά**, οπότε το `/push/config` επιστρέφει `enabled: false` και το UI κρύβει τη λειτουργία. Χρειάζεται `python -m scripts.vapid_keys` και `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT` στο περιβάλλον. |
| **Scraper σε συνεχή λειτουργία** | Η υπηρεσία υπάρχει στο compose αλλά απαιτεί `SCRAPER_USER_AGENT` με **πραγματικό email** — το compose αρνείται να ξεκινήσει χωρίς αυτό, επίτηδες. Ένας webmaster που θέλει να το σταματήσει δεν πρέπει να μαντεύει ποιος ρωτάει. |

---

## 3. Οθόνες του design που δεν χτίστηκαν

| Οθόνη | Τι είναι | Γιατί όχι |
|---|---|---|
| **C1, CD** | Κύπελλο | Δεν υπάρχουν δεδομένα (βλ. παραπάνω) |
| **H1** | Ιστορικό σεζόν ομάδας | Υπάρχει επιλογέας σεζόν στη σελίδα σωματείου, αλλά όχι η διάταξη της H1 |
| **AB1** | Σχετικά / επικοινωνία | Δεν χτίστηκε. Στατική σελίδα, μισή μέρα |
| **L2** | Live · φωτογραφία | Θέλει αποθήκευση αρχείων |
| **S3, S5** | Εικόνες MVP και «Σαν σήμερα» με κουμπί «Κατέβασμα εικόνας» | Οι S1, S2, S4 έγιναν. Αυτές οι δύο θέλουν και κουμπί λήψης στη σελίδα |
| **SD** | Αναζήτηση σε desktop **ως modal** με πλοήγηση ↑↓, Enter, Esc | Έγινε ως σελίδα με συντόμευση «/». Το modal και τα βελάκια λείπουν |

### Μικρότερα, από τις περιγραφές του README

- **Tabs στη σελίδα σωματείου** (Αγώνες / Ρόστερ / Ιστορικό / Κόντρα). Το
  `/kontra` υπάρχει ως δική του σελίδα· τα tabs όχι.
- **«Ενημερώνει: … · πριν Χ΄»** στη live σελίδα — ποιος καταγράφει και πόσο
  πρόσφατα.
- **SSE** αντί για polling. Το README δέχεται και τα δύο· τώρα τρέχει polling
  κάθε 15 δευτερόλεπτα.
- **Φωτογραφία από τον εθελοντή** — ίδιο εμπόδιο με τις υπόλοιπες φωτογραφίες.
- **Οθόνη διαχείρισης για MVP** — το API υπάρχει (`POST /editor/mvp`), αλλά η
  ένωση το καλεί μόνο με curl. Χρειάζεται φόρμα στο `/diaxeirisi`.
- **Οθόνη διαχείρισης για κωδικούς σωματείων** — ίδιο: το API υπάρχει
  (`/editor/club-codes`), η φόρμα όχι.
- **Οθόνη διαχείρισης για σύντομα ονόματα και ζώνες** — τα δύο πεδία που
  χρειάζονται ανθρώπινη καταχώριση και δεν έχουν πού να καταχωρηθούν.

---

## 4. Ανάπτυξη

Ο server **τρέχει build πριν τον επανασχεδιασμό**. Ό,τι περιγράφεται εδώ
δοκιμάστηκε τοπικά στο `http://127.0.0.1:3000`.

Πριν το επόμενο deploy:

1. **Τρία migrations** δεν έχουν εφαρμοστεί εκεί:
   `012af0ce236d` (κωδικοί σωματείων), `0ea059ae6499` (προτιμήσεις
   ειδοποιήσεων), `65bae4f07b7b` (ψηφοφορία MVP).
2. **`NEXT_PUBLIC_SITE_URL`** πρέπει να οριστεί στο build του web, αλλιώς κάθε
   `og:image` δείχνει σε `localhost` και τα μοιρασμένα λινκ μένουν χωρίς
   εικόνα. Είναι πλέον build arg· ήταν το bug #1 του audit.
3. **Backfill σύντομων ονομάτων**:
   `python -m scripts.backfill_short_names --association epsip-ipeirou --write`
4. Η διεύθυνση `178-238-227-37.sslip.io` είναι **προσωρινή**. Θέλει κανονικό
   όνομα πριν μοιραστεί σε κόσμο.

---

## 5. Τι έγινε — για αναφορά

Ώστε να μη χαθεί το νήμα: βήματα 1–6 του handoff είναι υλοποιημένα.

- **Bugs του audit**: `og:image` (ήταν ρύθμιση, όχι κώδικας), live status χωρίς
  έλεγχο ημερομηνίας (λύθηκε στα άκρα του API με παράθυρο 3 ωρών),
  `aria-hidden` (ήταν ήδη).
- **Πλοήγηση**: Αρχική · Αγώνες · Βαθμολογία · Ομάδες · Περισσότερα, με SVG
  εικονίδια και cookie `ps_league` που κρατά την κατηγορία.
- **Σελίδες στο design**: αρχική, αγώνες, βαθμολογία, αγώνας, σωματείο, γήπεδο,
  αναζήτηση, καλωσόρισμα, εθελοντής, ειδοποιήσεις, MVP, σφάλματα, dark mode —
  σε κινητό και desktop.
- **Social**: S1, S2, S4 με τη γραμματοσειρά της μάρκας.
- **Πολλαπλά σωματεία**, προτιμήσεις ειδοποιήσεων ανά ομάδα και γεγονός,
  ήσυχες ώρες με σωστό χειρισμό μεσάνυχτων και ζώνης ώρας.
- **Live**: αναβολή/διακοπή με αιτιολογία, φύλλο σκόρερ με ρόστερ, αυτόματο
  λεπτό από τη σέντρα, αναίρεση 60 δευτερολέπτων.

Bugs που βρέθηκαν γράφοντας και διορθώθηκαν: βαθμολογία που έσκαγε σε αγώνα
χωρίς σκορ (το φίλτρο SQL ήταν ψεύτικη εγγύηση με `autoflush=False`), unique
constraint που εμπόδιζε επανέκδοση κωδικού, λατινικά γράμματα μέσα σε ελληνικά
ονόματα του μητρώου, slug διοργανώσεων μοναδικά μόνο μέσα σε σεζόν, manifest
που δήλωνε στρογγυλεμένο εικονίδιο ως maskable.
